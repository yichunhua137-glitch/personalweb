Home page image slots still using this folder:

- location.jpg: location card background image.
- waterloo.jpg: education card background image.

These files are referenced from CSS as `/home/<filename>`.

Already connected elsewhere:

- Avatar uses `/heading.jpg`.
- Featured visual uses one image from `/my photos/`.
